/**
 * App do mecânico (web mobile). Etapa 6.
 *
 * Build React separada, servida pelo Fastify do processo main na porta 4570,
 * acessada pelo celular no Wi-Fi da oficina. Compartilha domínio e tokens com
 * o desktop via @jjs/core e @jjs/ui — nada de regra duplicada.
 */
export const ETAPA = 6 as const;
